# Общие зависимости
requests>=2.31
anthropic>=0.40
python-dotenv>=1.0

# python_bot/ — чистый Python-бот (без TradingView, без numpy/pandas/yfinance —
# работает на любом CPU, включая старые процессоры без SSE4.1/AVX)

# webhook_relay.py — legacy-вариант через TradingView webhook (нужен платный план)
flask>=3.0
